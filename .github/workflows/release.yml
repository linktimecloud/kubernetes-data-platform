name: Release

on:
  push:
    branches:
      - main
      - release-*
    tags:
      - "v*"
  workflow_dispatch: { }

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
      actions: read
      checks: write
      issues: read
      packages: write
      pull-requests: read
      repository-projects: read
      statuses: read
    runs-on: ubuntu-22.04
    name: goreleaser
    steps:
      - name: Checkout
        uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608
        with:
          fetch-depth: 0
      - run: git fetch --force --tags
      - name: Build CLI and Push CLI
        run: |
          make kdp-cli-build
          make kdp-cli-push SERVER_USER=${{ secrets.REG_USER }} SERVER_PASSWORD=${{ secrets.REG_PASSWD }} SERVER_URL= ${{ secrets.ARTIFACT_SERVER_PULL }}

      - name: Docker Login
        uses: docker/login-action@343f7c4344506bcbf9b4de18042ae17996df046d # v3.0.0
        with:
          username: ${{ secrets.REG_USER }}
          password: ${{ secrets.REG_PASSWD }}

      - name: Build Images
        id: build
        run: |
          make docker-build IMG_REGISTRY=${{ secrets.CONTAINER_REGISTRY }}

      - name: Push Images
        run: |
          make docker-push IMG_REGISTRY=${{ secrets.CONTAINER_REGISTRY }}

      - name: Set up Go
        uses: actions/setup-go@0c52d547c9bc32b1aa3301fd7a9cb496313a4491
        with:
          go-version: 1.21
          cache: true
      - uses: goreleaser/goreleaser-action@7ec5c2b0c6cdda6e8bbb49444bc797dd33d74dd8 # v5.0.0
        with:
          distribution: goreleaser
          version: 1.24.0
          args: release --clean --timeout 60m
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
